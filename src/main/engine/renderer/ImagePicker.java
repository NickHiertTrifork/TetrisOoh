package main.engine.renderer;

import java.util.Base64;

/**
 *
 * Picks image from a spritesheet based on the input number:
 * 1 = top left,
 * maximum row on column 1 = top right.
 *
 */

public class ImagePicker {

    /**
     * figure out how to pass the selected image to the renderer (what type, Base64??)
     */

    public static Base64 getImage(int index) {

        //1. read file
        //
        //2. find x&y coords of index (probably hardcoded.. i.e. 32x32)
        //
        //3. convert the image to a sub-image to return

        return null;
    }
}
